<% if work.representative.present? && (gf = ::FileSet.load_instance_from_solr(work.representative)) %>
  <%= media_display gf, transcript_id: work.tei_id %>
<% else %>
  <%= image_tag 'nope.png', class: "canonical-image" %>
<% end %>
